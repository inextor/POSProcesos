import { CustomToTitlePipe } from './custom-to-title.pipe';

describe('CustomToTitlePipe', () => {
  it('create an instance', () => {
    const pipe = new CustomToTitlePipe();
    expect(pipe).toBeTruthy();
  });
});
